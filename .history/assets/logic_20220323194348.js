//define basic variables
//create an array for each collumn of the table
//sort that array into decennding order
//display the difference between each as the pin, a zero differece results in a null display, or rather only show differences of zero if zero is defined as an actual cut on the key. 